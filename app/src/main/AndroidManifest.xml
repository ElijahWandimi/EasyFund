<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:tools="http://schemas.android.com/tools">

<uses-permission android:name="android.permission.INTERNET" />

<application
    android:allowBackup="true"
    android:dataExtractionRules="@xml/data_extraction_rules"
    android:fullBackupContent="@xml/backup_rules"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:supportsRtl="true"
    android:theme="@style/Theme.Material3.Dark"
    tools:targetApi="31">
    <activity
        android:name=".ui.notifications.DonationEventActivity"
        android:exported="false" />
    <activity
        android:name=".ui.SplashScreenActivity"
        android:exported="false" />
    <activity
        android:name=".DashActivity"
        android:exported="false"
        android:label="@string/title_activity_dash" />
    <activity
        android:name=".authorise.RegisterActivity"
        android:exported="false"/>
    <activity
        android:name=".authorise.LoginActivity"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <activity
        android:name=".MainActivity"
        android:exported="true"
        />

    <activity
        android:name=".ui.create_event.CreateEventActivity"
        android:exported="false"
        />
</application>

</manifest>
